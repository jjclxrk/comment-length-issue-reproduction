// This single-line comment is intentionally made long enough to exceed the default
// maximum comment length and trigger the ESLint comment-length rule for single-line comments.
// It should demonstrate that the rule correctly warns about overly long comments.
export function example() {
  return "Hello, world!";
}
